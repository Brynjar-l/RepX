
# --- TomcatWebServer ---
server.port=${PORT:8080}

# --- DB ---
spring.datasource.url=jdbc:postgresql://dpg-d46vrb9r0fns73bh8peg-a.frankfurt-postgres.render.com:5432/repx_m38y?sslmode=require
spring.datasource.username=team21
spring.datasource.password=YaJi65gGInchVGGakurOeS9JC7q9dYEv

spring.datasource.driver-class-name=org.postgresql.Driver

# Let JPA create/adjust tables while you're testing:
spring.jpa.hibernate.ddl-auto=validate
# If you already applied my SQL schema manually, switch to:
# spring.jpa.hibernate.ddl-auto=validate

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.open-in-view=false
spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}

# Logging (optional)
logging.level.org.springframework.jdbc.core=INFO
logging.level.org.hibernate.SQL=INFO
